services:
  admin-app:
    build:
      context: /opt/masyg-art-monorepo     # 👈 repo root
      dockerfile: apps/admin/Dockerfile    # 👈 per-app Dockerfile path
    container_name: admin-app
    restart: unless-stopped
    env_file: [.env]
    expose: ["3002"]
    networks: [public]

networks:
  public:
    external: true
