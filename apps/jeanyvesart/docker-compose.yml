services:
  admin-app:
    build:
      context: /opt/masyg-art-monorepo     # 👈 repo root
      dockerfile: apps/jeanyvesart/Dockerfile    # 👈 per-app Dockerfile path
    container_name: jeanyvesart-app
    restart: unless-stopped
    env_file: [.env]
    expose: ["3001"]
    networks: [public]

networks:
  public:
    external: true
