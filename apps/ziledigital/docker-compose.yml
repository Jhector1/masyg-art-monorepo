services:
  admin-app:
    build:
      context: /opt/masyg-art-monorepo     # 👈 repo root
      dockerfile: apps/ziledigital/Dockerfile    # 👈 per-app Dockerfile path
    container_name: ziledigital-app
    restart: unless-stopped
    env_file: [.env]
    expose: ["3003"]
    networks: [public]

networks:
  public:
    external: true
